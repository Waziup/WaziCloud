
get:
  tags: [data]
  responses:
    200:
      schema:
        items:
          $ref: ! '../definitions.yaml#/Observations'
        type: array
      description: 'get list of all observations'
  produces:
  - application/json
