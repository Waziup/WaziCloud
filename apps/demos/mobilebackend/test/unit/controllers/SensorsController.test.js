"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/SensorsController');

describe('controllers:SensorsController', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
